# {機能名} API

{この機能グループの概要を1〜2文で記述}

## 共通エラーレスポンス

<!-- 該当するものだけ残す。エラーコードは README.md のエラーコード一覧に準拠。 -->

全エンドポイント共通:
- `401 Unauthorized` (`UNAUTHORIZED`): {条件}

ID指定系エンドポイント（`:id` を含むもの）のみ:
- `404 Not Found` (`{RESOURCE}_NOT_FOUND`): {条件}

---

## {番号}. {操作名}

### `{METHOD} /api/{path}`

{このエンドポイントが何をするかを1〜2文で記述}

#### リクエストパラメータ

<!-- Query / URL / Body のうち該当するものだけ残す。不要なブロックは削除し、すべて不要な場合のみ「なし」を残す。 -->

**(URLパラメータ)**

- `:id` - {説明}

**(リクエストボディ - JSON)**

| フィールド | 型 | 必須 | 制約 | 説明 |
| --- | --- | --- | --- | --- |
| `field` | `string` | Yes | 最大50文字。trim される。 | {説明} |

<!-- パラメータが存在しないエンドポイントでは、上記ブロックを削除したうえで「なし」だけを残す -->

#### バリデーション・制約

- {入力値に対するサーバー側の検証ルールを箇条書きで記述}

#### 内部処理（実装者向け）

<!-- 副作用やDBへの書き込みなど、レスポンスだけでは読み取れないサーバー内部の処理がある場合のみ記述する -->

1. {手順1}
2. {手順2}

#### レスポンス

**正常系**

- `200 OK` / `201 Created`: {どのような場合に返るか}

**(レスポンスボディ - JSON)**

<!-- レスポンス表の「必須」列は、リクエストとは意味が異なり「この成功レスポンスに常に含まれるか」（Yes / No）を表す。null 可否や形式は「制約」「説明」に書く。 -->

| フィールド | 型 | 必須 | 制約 | 説明 |
| --- | --- | --- | --- | --- |
| `field` | `string` | Yes | {形式・値域・他フィールドとの関係など} | {説明} |

<!-- 配列やネストしたオブジェクトの要素を列挙する場合: トップレベルは上表と下記 JSON でよいことが多い。各要素のフィールドは `home.md` と同様、見出し（例: **`items` 配列**）+ 表（必要ならフィールド名・型・説明の3列）で補足する。JSON 全体の前後どちらに置くかは読みやすさで選んでよい。 -->

レスポンス例
```jsonc
{
  // レスポンス例（RFC 3339 date-time は例では +09:00 で記述する。詳細は README.md）
}
```

**異常系**

<!-- エラーコードは README.md のエラーコード一覧に準拠。エラーボディは README の統一フォーマットに従う。フィールドを個別に説明する必要がある場合のみ、上記と同様の表を追加する。 -->
- `400 Bad Request` (`INVALID_REQUEST`): {条件}
- `409 Conflict` (`{ERROR_CODE}`): {条件}

#### 冪等性

<!-- 冪等性の保証がある場合のみ記述する -->
{冪等である場合、再実行時の挙動を明記する}
