# Database (compose.yml の db サービス利用時: ホスト名は db、POSTGRES_DB の値に合わせる)
DATABASE_URL=postgresql://user:password@db:5432/dailygreen

# Better Auth (32+ chars). Generate: openssl rand -base64 32
BETTER_AUTH_SECRET=

BETTER_AUTH_URL=http://localhost:5173

# Google OAuth
# リダイレクトURI: {BETTER_AUTH_URL}/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
